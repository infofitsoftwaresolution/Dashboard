# ============================================
# PostgreSQL Database Configuration
# ============================================
# Copy this file to .env and fill in your database credentials

# Database Host (REQUIRED)
# Example: dashboard.c3ea24kmsrmf.ap-south-1.rds.amazonaws.com
# Or: localhost (if running locally)
DB_HOST=your-database-host.rds.amazonaws.com

# Database Port (REQUIRED)
# Default PostgreSQL port is 5432
DB_PORT=5432

# Database Name (REQUIRED)
# Example: postgres
DB_NAME=postgres

# Database Username (REQUIRED)
# Example: postgres
DB_USER=postgres

# Database Password (REQUIRED)
# ⚠️ Keep this secure and never commit to Git
DB_PASSWORD=your_password_here

# Table Name (REQUIRED)
# The name of the table in your PostgreSQL database
# Default: audittrail_firehose
TABLE_NAME=audittrail_firehose

# ============================================
# Optional Configuration
# ============================================

# Log Level (OPTIONAL)
# Options: DEBUG, INFO, WARNING, ERROR
# Default: INFO
LOG_LEVEL=INFO
